/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['graph.facebook.com', 'platform-lookaside.fbsbx.com', 'pub-placeholder.r2.dev'],
  },
}
module.exports = nextConfig
